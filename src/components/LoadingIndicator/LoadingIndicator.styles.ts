import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  fullPage: {
    position: 'absolute',
    height: '100%',
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
    alignSelf: 'center',
  },
  custom: {
    justifyContent: 'center',
    alignItems: 'center',
  },
});
